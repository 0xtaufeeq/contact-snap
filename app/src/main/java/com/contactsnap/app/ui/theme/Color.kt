package com.contactsnap.app.ui.theme

import androidx.compose.ui.graphics.Color

// Soft indigo / periwinkle accent.
val AccentDark = Color(0xFF8B9DF7)
val OnAccentDark = Color(0xFF11142E)

// Dark (default) — refined near-black with a faint cool cast.
val BgDark = Color(0xFF0E0E12)
val CardDark = Color(0xFF18181E)
val CardHighDark = Color(0xFF1B1F3A)   // indigo-tinted (avatars, selected)
val TextDark = Color(0xFFF4F3EF)
val TextMutedDark = Color(0xFF9594A0)
val OutlineDark = Color(0xFF2A2A32)
val OutlineDimDark = Color(0xFF1E1E24)

// AMOLED — pure black canvas for OLED screens.
val BgAmoled = Color(0xFF000000)
val CardAmoled = Color(0xFF0D0D11)
val CardHighAmoled = Color(0xFF141733)
val OutlineAmoled = Color(0xFF1C1C22)
val OutlineDimAmoled = Color(0xFF141418)

// Light — cream canvas, white cards, deeper indigo for contrast.
val AccentLight = Color(0xFF5061D6)
val OnAccentLight = Color(0xFFFFFFFF)
val BgLight = Color(0xFFFBFAF8)
val CardLight = Color(0xFFFFFFFF)
val CardHighLight = Color(0xFFE4E7FB)
val TextLight = Color(0xFF1A1A1A)
val TextMutedLight = Color(0xFF6B6A66)
val OutlineLight = Color(0xFFE7E4DD)
val OutlineDimLight = Color(0xFFEFEDE8)

// Amber used to flag low-confidence fields (kept distinct from the accent).
val WarnAmber = Color(0xFFE0A93B)
